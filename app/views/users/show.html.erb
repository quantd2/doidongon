<% provide(:title, @user.email) %>

<div class="row">
  <% if @user.items.any? %>
    <h3> My Items (<%= Item.count %>)</h3>
    <div class="my-items">
      <%= render @items %>
    </div>

  <%= will_paginate @items %>
  <% end %>
</div>
